# Generated by IBM TransformationAdvisor
# Fri Jul 05 01:56:53 UTC 2019

apiVersion: v1
kind: Service
metadata:
  name: servletsample
  labels:
    app: servletsample
spec:
  ports:
    - port: 9080
  selector:
    app: servletsample
  type: NodePort

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: servletsample
  name: servletsample
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: servletsample
    spec:
      containers:
      - image: mycluster.icp:8500/default/servletsample:latest
        name: servletsample
        env:
          - name: WLP_LOGGING_CONSOLE_FORMAT
            value: json
          - name: WLP_LOGGING_CONSOLE_LOGLEVEL
            value: info
          - name: WLP_LOGGING_CONSOLE_SOURCE
            value: message,trace,accessLog,ffdc
        ports:
        - containerPort: 9080
          protocol: TCP
